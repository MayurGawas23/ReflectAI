{
  "entities": {
    "Interaction": {
      "title": "Interaction",
      "description": "User journal reflection session containing prompts and Gemini responses",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique session identifier"
        },
        "userId": {
          "type": "string",
          "description": "UID of the owning authenticated user"
        },
        "title": {
          "type": "string",
          "description": "Title or headline of the reflection session",
          "maxLength": 200
        },
        "summary": {
          "type": "string",
          "description": "Synthesized summary generated by Gemini"
        },
        "actionItems": {
          "type": "array",
          "description": "List of action steps or pondering questions"
        },
        "themes": {
          "type": "array",
          "description": "List of key cognitive theme tags"
        },
        "location": {
          "type": "object",
          "description": "Optional attached geographic place where the reflection occurred"
        },
        "messages": {
          "type": "array",
          "description": "Chronological multi-turn interaction dialogue messages"
        },
        "createdAt": {
          "type": "number",
          "description": "Creation unix epoch timestamp in milliseconds"
        },
        "updatedAt": {
          "type": "number",
          "description": "Last updated unix epoch timestamp in milliseconds"
        }
      },
      "required": ["id", "userId", "title", "messages", "createdAt", "updatedAt"]
    },
    "AdminRecord": {
      "title": "AdminRecord",
      "description": "Registry of authorized administrators with RBAC privileges",
      "type": "object",
      "properties": {
        "email": { "type": "string" },
        "grantedAt": { "type": "number" },
        "role": { "type": "string" }
      },
      "required": ["email", "role"]
    },
    "SystemTelemetry": {
      "title": "SystemTelemetry",
      "description": "Anonymized aggregate usage metrics, model performance, and system health",
      "type": "object",
      "properties": {
        "totalReflectionsTracked": { "type": "number" },
        "totalSynthesizedSessions": { "type": "number" },
        "totalLocationsPinned": { "type": "number" },
        "activeUsersCount": { "type": "number" },
        "lastUpdated": { "type": "number" }
      }
    }
  },
  "firestore": {
    "/users/{userId}/interactions/{interactionId}": {
      "schema": "Interaction",
      "description": "User-isolated journal entries and reflection conversations with Gemini"
    },
    "/admins/{adminId}": {
      "schema": "AdminRecord",
      "description": "Registry of verified platform administrators"
    },
    "/system_telemetry/{metricId}": {
      "schema": "SystemTelemetry",
      "description": "Aggregated platform telemetry data accessible solely by admins"
    }
  }
}
